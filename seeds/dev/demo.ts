/* Dev-only demo gym: users, staff, members, equipment, programs, history.
 * NEVER loaded in production (guarded in seed-cli). */

export async function seedDemo(): Promise<void> {
  console.log('[seed] demo gym: not yet implemented');
}
