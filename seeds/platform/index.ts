/* Platform content: taxonomies + curated exercise library + graph edges +
 * default screening/waiver templates + default progression rules.
 * Idempotent: safe to re-run. Populated with real content in the seeds task. */

export async function seedPlatform(): Promise<void> {
  console.log('[seed] platform content: not yet implemented');
}
